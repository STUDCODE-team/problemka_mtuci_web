import 'package:flutter/material.dart';

class AppRadius {
  static const double radiusBase = 10;
  static const double radiusSm = 6;
  static const double radiusMd = 8;
  static const double radiusLg = 10;
  static const double radiusXl = 14;
  static const double radiusFull = 9999;

  static BorderRadius circular = BorderRadius.circular(radiusBase);
  static BorderRadius circularSm = BorderRadius.circular(radiusSm);
  static BorderRadius circularMd = BorderRadius.circular(radiusMd);
  static BorderRadius circularLg = BorderRadius.circular(radiusLg);
  static BorderRadius circularXl = BorderRadius.circular(radiusXl);
}
